# Fixture instructions

- This repository is a read-only review smoke fixture.
- Review only the confirmed remote patch under `.review/pr-123/`.
- Do not modify files, branches, commits, or external state.
- Return the requested Markdown artifact as the final response.
