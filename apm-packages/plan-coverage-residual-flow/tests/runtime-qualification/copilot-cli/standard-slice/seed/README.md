# STD-001 seed

Incomplete single-process configuration loader fixture for Plan Coverage standard-slice runtime qualification.

Source requirement is in the sibling `request.md`. The external oracle is `../verify.ps1` (copied into the worktree as `tests/verify-std-001.ps1` by the harness and hash-checked).
